# Target Repositories
# Copy to targets.yaml and customize
#
# Each target defines a repository the QA agents will scan and fix.

targets:
  - name: my-web-app
    path: /home/user/projects/my-web-app
    language: python
    default_departments: [qa, seo, ada, compliance, monetization, product]
    # Linter command (run during QA scan)
    lint_command: "ruff check ."
    # Test command (run during QA scan and after fixes)
    test_command: "python3 -m pytest tests/ -q"
    # Optional coverage command (used for CI scaffolding and regression reporting)
    coverage_command: "python3 -m pytest --cov=. --cov-report=term --cov-report=json:coverage.json"
    # Deploy command (run after fixes are pushed)
    deploy_command: "bash scripts/deploy.sh"
    # Extra context for the QA agent
    context: |
      This is a serverless web app using AWS Lambda, DynamoDB, and S3.
      Focus on: security vulnerabilities, input validation, error handling,
      performance bottlenecks, and AWS best practices.

  # Astro or other frontend-only repos can still participate in regression
  # coverage if they expose an explicit coverage command.
  # - name: codyjo.com
  #   path: /home/user/projects/codyjo.com
  #   language: astro
  #   default_departments: [qa, seo, ada, compliance, monetization, product]
  #   lint_command: "npm run check"
  #   test_command: "npm test && npm run build"
  #   coverage_command: "npm run test:coverage"
  #   deploy_command: "npm run build"
  #   context: "Portfolio and company hub site."

  # - name: another-repo
  #   path: /home/user/projects/another-repo
  #   language: javascript
  #   default_departments: [qa, product]
  #   lint_command: "npx eslint src/"
  #   test_command: "npm test"
  #   coverage_command: "npm run test:coverage"
  #   deploy_command: "npm run deploy"
  #   context: "React frontend with Node.js backend"

  # Terraform repos sometimes live at repo root on dev machines but inside a
  # terraform/ subdirectory in other environments. Keep the command relative
  # to the target repo instead of hardcoding a machine-specific absolute path.
  # - name: infra-repo
  #   path: /home/user/projects/infra-repo
  #   language: terraform
  #   default_departments: [qa, compliance, product]
  #   lint_command: "if [ -d terraform ]; then terraform -chdir=terraform validate; else terraform validate; fi"
  #   test_command: "if [ -d terraform ]; then terraform -chdir=terraform validate; else terraform validate; fi"
  #   deploy_command: "if [ -d terraform ]; then terraform -chdir=terraform validate; else terraform validate; fi"
  #   context: "Terraform infrastructure repo with environment-specific layout."
